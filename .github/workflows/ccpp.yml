on:
  push:
    branches:
    - '*'
  pull_request:
    branches:
    - '*'
  release:
    types:
      - published

jobs:
  base:
    name: Base library ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: ['ubuntu-latest', 'macos-latest', 'windows-latest']
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v3
        name: Install Python
        with:
          python-version: '3.10'
      - name: Checkout submodules
        run: git submodule update --init --recursive

      - uses: ilammy/msvc-dev-cmd@v1
        if: matrix.os == 'windows-latest'

      - name: Install CMake and Ninja
        run: |
            which python
            python --version
            pip install cmake ninja
      - name: Configure static
        shell: bash -l {0}
        run: |
            if [ "$RUNNER_OS" == "Windows" ]; then
              export CC=cl.exe
              export CXX=cl.exe
            fi
            cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -Dgtest_force_shared_crt=ON -DBUILD_SHARED_LIBS=OFF
      - name: Configure shared
        if: matrix.os != 'windows-latest'
        shell: bash -l {0}
        run: |
            cmake -S . -B build-shared -G Ninja -DCMAKE_BUILD_TYPE=Release -Dgtest_force_shared_crt=ON -DBUILD_SHARED_LIBS=ON
      - name: Build static
        shell: bash -l {0}
        run: |
            cmake --build build
      - name: Build shared
        if: matrix.os != 'windows-latest'
        shell: bash -l {0}
        run: |
            cmake --build build-shared
      - name: Test static
        shell: bash -l {0}
        run: |
            cd build
            ctest
      - name: Test shared
        if: matrix.os != 'windows-latest'
        shell: bash -l {0}
        run: |
            cd build
            ctest build-shared

